{
  "name": "auromono",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --watch index.ts",
    "build": "bun run build.ts",
    "serve": "bun run serve.ts",
    "start": "bun run index.ts"
  },
  "devDependencies": {
    "bun": "latest",
    "bun-types": "latest"
  }
}