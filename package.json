{
  "name": "auromono",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "vite",
    "build": "lerna run build",
    "serve": "web-dev-server --node-resolve --open --watch",
    "start": "npm run dev"
  },
  "devDependencies": {
    "lerna": "^5.1.0",
    "vite": "^4.3.0",
    "@web/dev-server": "^0.1.35"
  }
}